# 2026-09-27：个人审美库与轻量筛图工作流

## 用户意图（实现前记录）

> 图片首先是独立资产；搜索角色只是发现上下文。第一轮只负责审美筛选，不当数据库管理员。
> 独立的“我的审美库”；本角色参考 / 通用灵感 / 待定 / 淘汰；一张精选图对应一张现场卡，不强迫所有保留图制卡。
> BrowserSkill + Codex / Antigravity 是默认采集、分析路径，不额外配置独立 OpenAI API。

继续 `codex/reference-library-rebuild`，起点 `b95f37540ada1bae18de949873bdbbcce021e621`。保留现有存储去重与安全门槛，不重写正确模块。原项目、批次、素材和用户选择不得丢失；搜索意图不提升为图像事实；AI 只能产生草稿，不得自行确认。

## 可观察验收条件

- 缩略图点击、上下张、K/M/X/自动下一张保持列表节点及合理滚动位置；大量图片分页仍可靠。
- 淘汰立即从正常流隐藏，可恢复；一个角色淘汰不能损害其他角色或全局收藏。
- 全局审美收藏不依附任何角色；同一个 Asset 能被多个项目复用而不复制字节。
- 发现来源/任务上下文、图像事实、项目使用关系、审美判断概念分开；保留旧选择和来源。
- 明确的精选、制卡、草稿检查、独立确认路径；保留人工纠错；一图一卡。
- 默认 BrowserSkill 任务包保留完整自由要求、三层发现意图、多语言检索、来源与阻断/缺口报告；不依赖付费 API。
- 数据迁移幂等，回归测试覆盖并发修订、刷新持久化与旧卡安全门槛；实际运行 UI。
- 文档与实现同步，提交后核对远端 SHA，不合并 main。

## 工作区与环境

GitHub 接口确认目标 SHA；用户本地 Codex 连接失败，无法读取或触碰用户本机未提交工作。执行容器直连 GitHub 的 `git fetch` 因 DNS 失败。通过独立临时 `ops/reference-workspace-20260927` 分支的只读 CI 获取目标历史 Git bundle，在隔离目录克隆；本地 HEAD 与目标 SHA 相同，初始工作区干净。临时分支不合并目标或 main。原始素材与测试截图不作为本轮新增公开内容。

## 结果（执行后追加）

尚在实现与验证中；本节仅追加实际执行结果，不将计划当通过。

### 本地实现与循环复测（提交前 checkpoint）

保留 CAS 存储与安全门槛，新增独立 inspirations、discoveries、asset_observations；旧 Reference 作为项目使用关系保留。默认流隐藏淘汰、恢复前一选择、跨项目/全局引用保护、显式延迟清理与备份互锁。采集/分析包冻结完整要求，支持真实回执、分次分析与明确等待状态。默认 Agent + BrowserSkill，无独立 API 配置入口。前端稳定列表、四个筛选动作、独立全局导航、单步制卡和来源纠错。文档同步至 v0.3。

实际复现旧 bug：滚动 3100 后点击缩略图，整个节点被替换，scrollLeft=0。修后保留原节点和未变化缩略图；已可见项点击滚动精确不变，键盘和自动下一张保持当前可见。循环中又修复淘汰后的瞬时首图回退、已存在草稿重新分析时隐藏等待任务、预览短暂加载失败后仍隐藏图片，以及旧执行器重复提交完成状态的兼容冲突。

已运行：

- 原后端 64 项通过；加入个人库测试后，核心命令 **84 passed**（6.22 秒）。覆盖迁移、全局库、多角色引用、回收、快照、分析包、CLI 和无独立 API 流程。
- 更新后的组件回归 **8 passed**（23.84 秒）；随后补预览恢复测试并单跑与刷新相关的两项 **2 passed**。最终合并组件数量及 HTTP CI 结果待后续追加，不预填通过。
- `tests/test_live_system_regression.py -k migration`：**1 passed, 1 deselected**（17.21 秒）。435 created / 53 existing，重复导入 0 / 488，缺图和错误均 0；keep47 / pending255 / reject133，默认流302。真实 schema1表升级、488别名、435份原图哈希、选择/来源/revision保留；未新增图像观察或现场卡。
- 将实际远端旧 schema1 数据副本 `.local/baseline-real` 与升级后 `.local/current-real` 逐项对照，435条原选择/来源/版本/图片绑定全部不变。
- 在独立 `.local/acceptance-real` 用真实历史图片走 Chromium DOM + TestClient：原图打开、后段缩略图不重建/不回首、全局收藏、第二角色引用、淘汰恢复、建立等待制卡任务、390px 布局、新浏览器页面恢复选中项和任务。资产数仍435，观察数仍0，doctor无缺失/损坏/外键错误。**这是实际 UI，但不是 HTTP/Cookie E2E**。
- `node --check web/app.js`、Python compileall、`git diff --check` 通过。截图与数据库只在私有 `.local`，不提交。

### 对旧验收脚本的修正

旧 `test_live_system_regression.py` 依赖固定 `/tmp` 数据库，还对历史图硬编码“真人/角色/灯光/自有授权”以生成已确认卡；这种结果不证明真实视觉或授权判断正确。本轮改为自足历史迁移与真实图片显示/使用关系测试，不向历史素材编造正向事实。卡片确认、离线和反例门槛使用明确合成样本，分别测试，不混淆。

### 外部与环境边界

本容器真实浏览器 HTTP 首次基线测试返回 `net::ERR_BLOCKED_BY_ADMINISTRATOR`；未规避管理策略。完整 HTTP/Cookie/上传下载/file:// 测试将通过正常 GitHub Actions 执行，结果后补；当前不宣称本容器通过。

匿名来源探测：小红书转登录，Pinterest/X 403，Instagram 429后停止，微博工具受限，Curecos工具不可访问；Cosplayers Archive照片入口、FilmGrab与ShotDeck公开主页可读。腾讯BrowserSkill官方协议已审阅，但用户登录态BrowserSkill、实际平台搜图质量/下载、真实Agent分析质量、Windows/手机离线实际打开、公开部署和Notion在线同步未验证。未调用独立付费API，未安装或接管用户浏览器。
