# Workspace instructions

For image collection, read notes/collection-workflow.md first. Capture minimal source metadata and exact visible currentSrc values during browser work, download in small batches with scripts/download_candidates.py, then do image analysis locally. Keep existing historical batches unchanged unless asked. Never use hidden APIs, cookies, or access-control workarounds.
